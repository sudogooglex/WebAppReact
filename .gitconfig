[user]
	name = sudogooglex
	email = business.contact.web@gmail.com
[credential]
	helper = cache --timeout=36000
[guitool "Add to .gitignore"]
	cmd = echo \"\n$FILENAME\" >> .gitignore & git add .gitignore
	needsfile = yes
	confirm = yes
